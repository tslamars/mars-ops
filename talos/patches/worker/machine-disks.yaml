machine:
  disks:  # Added to manage /dev/sdb
    - device: /dev/disk/by-id/scsi-0QEMU_QEMU_HARDDISK_drive-scsi1
      partitions:
        - mountpoint: /var/lib/longhorn
          label: longhorn-data